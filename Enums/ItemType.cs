namespace burgershack.Enums
{
    // NOTE magic strings === 'bad'
    // ProxyState.on('values')
    // ProxyState.on(ProxyState.values)
    public enum ItemType
    {   
        Burger = 100,
        Side,
        Drink
    }

}